[package]
name = "registry_checker"
version = "0.1.0"
edition = "2024"

[dependencies]
cargo_metadata = "0.18"
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
